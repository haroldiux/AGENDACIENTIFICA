<Archive: ui-overhaul-vercel-template>
## Final Status
Completed successfully.

## Summary of Changes
- Integrated `shadcn/ui`, `next-themes`, `framer-motion`, and `geist` fonts into the frontend.
- Overhauled `tailwind.config.js` and `globals.css` with semantic `shadcn/ui` color variables and UNITEPC branding (Purple and Cyan).
- Refactored `Sidebar.tsx` to use `framer-motion` for a collapsible, sleek navigation layout.
- Restyled the `react-big-calendar` instance in `CalendarView.tsx` and updated structural borders and events for light/dark mode support.
- Migrated native HTML elements to `shadcn/ui` components (`Button`, `Card`, `Dialog`, etc.) across `/actividades` and `/calendario` views.
- Fixed TypeScript, dependencies, and linting errors, including restoring the root layout template.
</Archive: ui-overhaul-vercel-template>
